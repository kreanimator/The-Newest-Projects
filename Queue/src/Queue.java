public interface Queue {

    static int AddQueue(int el) {
    }

    int AddQueue(int el);

    static int RemoveQueue(int el);


    int RemoveQueue(int el);

    boolean isEmpty();

    int AddQueue();
}